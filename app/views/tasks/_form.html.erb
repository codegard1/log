<%= form_for @task do |f| %>
	<!-- title, complete, completed_on, user_id, tasklist_id -->
	<%= f.hidden_field :tasklist_id, {:value => params[:task_list_id]} %>
	<% unless @task.complete then %>
		<%=f.hidden_field :complete, {:value => false} %>
	<% end %>

	<%= f.label :title %><br>
	<%= f.text_field :title %><br>
	<br>
	<%= f.label "Owner" %><br>
	<%= collection_select(
		:task,
		:user_id,
		User.all,
		:id,
		:name) %><br>
	<br>
	<%= f.submit "Save" %>
<% end %>
